## Summary

Fixes #79

Added a `BetSummary` component to the Registro view. This summary bar dynamically calculates and displays total picks, W/L/P counts, total staked, profit, and yield for the currently visible (filtered) bets.

## Changes
- Created `BetSummary.tsx` to display totals.
- Integrated `BetSummary` into `app/(dashboard)/registro/page.tsx` just below the filters.

## Testing
- All tests passing (`npx vitest run`)
- Prettier check: clean

## Checklist
- [x] Prettier formatted (`npx prettier --write`)
- [x] All tests passing
- [x] Conventional commit format
- [x] ≤ 20 files changed
- [x] No --force push
- [x] TypeScript strict, no `any`
- [x] SemVer: PATCH|MINOR|MAJOR
- [x] Version bumped in package.json & CHANGELOG.md updated
